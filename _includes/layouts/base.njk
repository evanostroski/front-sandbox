<!doctype html>
<html lang="en">
  <head>
    <title>{{ title }}</title>
    <script src="{{ scripts | safe }}"></script>
  </head>
  <body>
    <h1>{{ title }}</h1>
    <p>{{ description }}</p>

    {{ content | safe }}
  </body>
</html>

